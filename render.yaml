services:
  - type: web
    name: nexus-ai
    env: python
    buildCommand: pip install -r requirements.txt && python -c "from brain.trainer import train; train()"
    startCommand: gunicorn web_app:app --bind 0.0.0.0:$PORT --timeout 120